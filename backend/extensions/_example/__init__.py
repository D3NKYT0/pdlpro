"""Skeleton de extensão de cliente (não instalado por padrão).

Ative com::

    PDL_EXTENSION_APPS=extensions._example.apps.ExampleExtensionConfig

Rotas ficam em ``/api/v1/extensions/example_extension/``. Copie esta pasta para
``extensions/<cliente>/`` (sem o prefixo ``_``) ao criar uma extensão real.
"""
