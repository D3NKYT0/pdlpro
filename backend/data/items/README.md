Catálogo Lineage 2 (Interlude) usado para nome, grade e ícone dos itens.

Fonte: os mesmos XML do Omega Team (`backend/data/items`).
Ícones: `frontend/public/item-icons/{id}.jpg`.

Atualizar o JSON do frontend:

    cd frontend
    npm run catalog
